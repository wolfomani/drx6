"use client"

import ProfessionalCV from "@/components/professional-cv"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { Badge } from "@/components/ui/badge"
import { Progress } from "@/components/ui/progress"
import { ArrowLeft, Download, Mail, Phone, MapPin, Code, TrendingUp, Brain, Database, Globe, Zap, Users, Award, Github, ExternalLink, Heart, Sparkles, Target, BookOpen, Lightbulb, Rocket } from 'lucide-react'

export default function CVPage() {
  return <ProfessionalCV />
}
